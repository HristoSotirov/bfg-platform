LoginRequest:
  type: object
  description: Credentials used for login
  required:
    - username
    - password
  properties:
    username:
      type: string
      minLength: 1
      x-field-extra-annotation: '@NotBlank(message = "{auth.username.required}")'
      example: "admin"
    password:
      type: string
      format: password
      minLength: 1
      x-field-extra-annotation: '@NotBlank(message = "{auth.password.required}")'
      example: "admin123"

RefreshRequest:
  type: object
  description: Refresh token request
  required:
    - refreshToken
  properties:
    refreshToken:
      type: string
      minLength: 1
      x-field-extra-annotation: '@NotBlank(message = "{auth.refreshToken.required}")'

TokenResponse:
  type: object
  description: Access/refresh token response
  required:
    - accessToken
    - refreshToken
    - tokenType
  properties:
    accessToken:
      type: string
    refreshToken:
      type: string
    tokenType:
      type: string
      example: Bearer

ForgotPasswordRequest:
  type: object
  description: Request to send a password reset email
  required:
    - username
  properties:
    username:
      type: string
      minLength: 1

ResetPasswordRequest:
  type: object
  description: Request to reset password using a token
  required:
    - token
    - newPassword
  properties:
    token:
      type: string
      minLength: 1
    newPassword:
      type: string
      minLength: 8


